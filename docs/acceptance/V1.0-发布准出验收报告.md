# NSO 平台 V1.0 发布准出验收报告

验收日期：2026-09-12  
验收对象：`codex/security-review-fix` 工作区当前整体交付物  
正式入口：`nso-platform`、`nso-web`、canonical Flyway 目录

## 结论

**条件通过 / 暂不准出。** 代码级发布门禁通过，但尚未满足“真实试点证据齐全”的准出条件。当前不能认定为生产试点正式通过。

## 已验证通过

| 项目 | 证据 | 结论 |
| --- | --- | --- |
| 后端测试 | `scripts/verify-pilot.ps1`；Maven reactor，62 tests，Failures 0，Errors 0，Skipped 0 | 通过 |
| 前端类型与构建 | `npm run type-check`、`npm run build`；Vite 2503 modules，Initial JS 386 KiB gzip | 通过 |
| Flyway | `scripts/verify-migrations.ps1`；canonical V1-V39 连续，最新 V39 | 通过 |
| Compose | `docker compose config` | 通过 |
| 权限与业务矩阵 | `docs/testing/pilot-state-transition-matrix.md` 对应集成测试均在后端测试中执行 | 代码测试通过 |
| 文件安全 | 扩展名/MIME/内容签名、大小、项目数据范围及下载摘要校验均有实现和测试 | 代码测试通过 |

## 未验证及阻断项

| 阻断项 | 当前证据 | 准出要求 |
| --- | --- | --- |
| 试点环境与 TLS | `deploy/pilot/.env.pilot` 不存在；未提供 `tls.crt`、`tls.key` | 提供真实密钥、证书，执行 `preflight.ps1` |
| 运行入口安全 | 没有启动 pilot gateway/API；未取得 `/healthz`、readiness、未认证 401 和 `X-Trace-Id` 的现场响应 | 启动完整 Compose 并保存响应证据 |
| 依赖健康 | 当前只有独立的 MySQL/Redis/MinIO 容器，非 pilot stack；`minio-init` 状态未验证 | gateway/API/MySQL/Redis/MinIO healthy，`minio-init` exited(0) |
| 业务闭环 | 没有现场建档至归档录像、审计事件或手工回归记录 | 完成核心闭环并留档 |
| 备份恢复 | 仅审查了 `backup.ps1`、`restore-verify.ps1`，未执行真实备份恢复 | 记录 RPO/RTO、SQL 与 MinIO 完整性 |
| 性能 | `deploy/pilot/perf/k6-pilot.js` 存在，但没有 k6 结果、慢日志或执行计划 | 列表/详情 P95 < 2s，复杂统计 P95 < 5s |
| 监控告警 | `monitoring/alerts.yml` 存在，未接入告警平台，未取得触发截图 | 审计失败、调度失败、积压、慢 SQL、备份过期均有现场证据 |

## 整改顺序

1. 运维：补齐 `.env.pilot`、TLS 和 pilot Compose 启动记录，执行入口安全 smoke。
2. DBA/运维：执行异机备份、隔离恢复和 MinIO 校验，记录 RPO/RTO。
3. 开发/DBA：运行 k6 读写场景，补充 P95、错误率、慢日志和执行计划。
4. 运维：将 `alerts.yml` 接入 Prometheus/告警平台并验证五类告警。
5. 开发：处理 Flyway 运行时的 MySQL `VALUES()` 弃用告警、重复角色菜单和重复索引告警，并重新跑迁移验证。
6. 测试/产品：核对归档项目看板的逾期语义，完成现场业务闭环记录。

## 备注

工作区存在大量既有删除、修改和新增文件，本次按当前整体交付物验收，未回滚或清理任何未提交改动。`git diff --check` 仅发现需求规格文档第 161 行已有尾随空格。
